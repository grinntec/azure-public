## REFERENCE
# enter a descriptive name a good choice would be the subscription name
# only use letters, no special characters
ref=[value]

## AZURE LANDING ZONE
# these values are specific to the azure environment where the resources will be created
az_region=[value]
az_sub_id=[value]
az_tenant_id=[value]

## RESOURCE NAMES
# set the names of the resource group, storage account name, and blob container
# the resource group name will have the reference value appended
rg_name=rg-tfstate-
sa_name=satfstate
sacon_name=tfstatefiles
kv_name=kvtfstate

## tags
tag1=env=[value]
tag2=createdBy=[value]
